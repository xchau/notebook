# initialize new repo
git init

# add file to staging area
git add <file>

# view the staging area
git status

# commit the staging area to the master branch
git commit -m 'MESSAGE'

# view the master branch
git ll
